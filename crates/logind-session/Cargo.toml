[package]
name = "logind-session"
version = "0.2.0"
edition = "2021"
license = "MIT"
description = "Fail-closed systemd-logind session resolve and inhibitor RAII"
repository = "https://github.com/MasonRhodesDev/logind-session"
publish = true

[dependencies]
libc = "0.2"
zbus = { version = "5", default-features = false, features = ["tokio"] }
