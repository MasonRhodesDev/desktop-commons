[package]
name = "xdg-paths"
version = "0.2.0"
edition = "2021"
license = "MIT"
description = "Fail-closed XDG config/data/cache/runtime paths; no /tmp or /run/user fallbacks"
repository = "https://github.com/MasonRhodesDev/xdg-paths"
publish = true

[dependencies]
